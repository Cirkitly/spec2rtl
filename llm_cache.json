{
  "Given the following hardware specification, generate ONLY the synthesizable Verilog-2001 RTL code. Do not add any explanations. The output should be a single Verilog code block.\n\nSPECIFICATION:\n---\n### Module: UART Transmitter\n\n#### High-Level Description\nDesign a simple Universal Asynchronous Receiver-Transmitter (UART) module that only handles transmission (TX). It should be synthesizable and follow standard UART protocol.\n\n#### Functional Requirements\n- **Data Format**: 1 start bit, 8 data bits, 1 stop bit.\n- **Data Input**: An 8-bit parallel data input port `i_data`.\n- **Transmit Trigger**: A single-cycle pulse on `i_tx_start` should initiate the transmission of the data currently on `i_data`.\n- **Output**: A single serial output pin `o_tx_serial`.\n- **Status Signal**: An output signal `o_busy` that is high during transmission and low when idle.\n- **Reset**: An active-high synchronous reset `i_reset`.\n\n#### Clocking\n- The module should operate on a single clock `i_clk`.\n- A configurable `BAUD_RATE_DIVIDER` parameter should be used to control the bit rate. For a 50MHz clock and a 9600 baud rate, the divider would be approximately 50,000,000 / 9600 = 5208.\n": "```verilog\nmodule uart_tx #(\n    parameter BAUD_RATE_DIVIDER = 5208\n)(\n    input         i_clk,\n    input         i_reset,\n    input  [7:0]  i_data,\n    input         i_tx_start,\n    output reg    o_tx_serial,\n    output reg    o_busy\n);\n\n    // State encoding\n    localparam STATE_IDLE     = 2'd0;\n    localparam STATE_START    = 2'd1;\n    localparam STATE_DATA     = 2'd2;\n    localparam STATE_STOP     = 2'd3;\n\n    reg [1:0]   state, next_state;\n    reg [12:0]  baud_cnt;\n    reg [3:0]   bit_cnt;\n    reg [7:0]   data_latch;\n\n    // Next state logic\n    always @(*) begin\n        case (state)\n            STATE_IDLE:  next_state = (i_tx_start) ? STATE_START : STATE_IDLE;\n            STATE_START: next_state = (baud_cnt == BAUD_RATE_DIVIDER - 1) ? STATE_DATA : STATE_START;\n            STATE_DATA:  next_state = (baud_cnt == BAUD_RATE_DIVIDER - 1 && bit_cnt == 7) ? STATE_STOP : STATE_DATA;\n            STATE_STOP:  next_state = (baud_cnt == BAUD_RATE_DIVIDER - 1) ? STATE_IDLE : STATE_STOP;\n            default:     next_state = STATE_IDLE;\n        endcase\n    end\n\n    // Sequential logic\n    always @(posedge i_clk) begin\n        if (i_reset) begin\n            state       <= STATE_IDLE;\n            baud_cnt    <= 0;\n            bit_cnt     <= 0;\n            data_latch  <= 8'd0;\n            o_busy      <= 1'b0;\n            o_tx_serial <= 1'b1; // Idle is high\n        end else begin\n            state <= next_state;\n\n            case (state)\n                STATE_IDLE: begin\n                    baud_cnt    <= 0;\n                    bit_cnt     <= 0;\n                    o_tx_serial <= 1'b1; // Idle is high\n                    o_busy      <= 1'b0;\n                    if (i_tx_start) begin\n                        data_latch  <= i_data;\n                        o_busy      <= 1'b1;\n                    end\n                end\n\n                STATE_START: begin\n                    o_tx_serial <= 1'b0; // Start bit is low\n                    o_busy      <= 1'b1;\n                    if (baud_cnt == BAUD_RATE_DIVIDER - 1)\n                        baud_cnt <= 0;\n                    else\n                        baud_cnt <= baud_cnt + 1'b1;\n                end\n\n                STATE_DATA: begin\n                    o_tx_serial <= data_latch[bit_cnt];\n                    o_busy      <= 1'b1;\n                    if (baud_cnt == BAUD_RATE_DIVIDER - 1) begin\n                        baud_cnt <= 0;\n                        if (bit_cnt == 7)\n                            bit_cnt <= 0;\n                        else\n                            bit_cnt <= bit_cnt + 1'b1;\n                    end else begin\n                        baud_cnt <= baud_cnt + 1'b1;\n                    end\n                end\n\n                STATE_STOP: begin\n                    o_tx_serial <= 1'b1; // Stop bit is high\n                    o_busy      <= 1'b1;\n                    if (baud_cnt == BAUD_RATE_DIVIDER - 1)\n                        baud_cnt <= 0;\n                    else\n                        baud_cnt <= baud_cnt + 1'b1;\n                end\n\n                default: begin\n                    o_tx_serial <= 1'b1;\n                    o_busy      <= 1'b0;\n                end\n            endcase\n        end\n    end\n\nendmodule\n```",
  "You are a verification engineer. Your task is to write a SystemVerilog testbench.\n\nHere is the hardware specification:\n---\n### Module: UART Transmitter\n\n#### High-Level Description\nDesign a simple Universal Asynchronous Receiver-Transmitter (UART) module that only handles transmission (TX). It should be synthesizable and follow standard UART protocol.\n\n#### Functional Requirements\n- **Data Format**: 1 start bit, 8 data bits, 1 stop bit.\n- **Data Input**: An 8-bit parallel data input port `i_data`.\n- **Transmit Trigger**: A single-cycle pulse on `i_tx_start` should initiate the transmission of the data currently on `i_data`.\n- **Output**: A single serial output pin `o_tx_serial`.\n- **Status Signal**: An output signal `o_busy` that is high during transmission and low when idle.\n- **Reset**: An active-high synchronous reset `i_reset`.\n\n#### Clocking\n- The module should operate on a single clock `i_clk`.\n- A configurable `BAUD_RATE_DIVIDER` parameter should be used to control the bit rate. For a 50MHz clock and a 9600 baud rate, the divider would be approximately 50,000,000 / 9600 = 5208.\n\n---\nHere is the exact Verilog RTL for the module:\n---\n```verilog\nmodule uart_tx #(\n    parameter BAUD_RATE_DIVIDER = 5208\n)(\n    input         i_clk,\n    input         i_reset,\n    input  [7:0]  i_data,\n    input         i_tx_start,\n    output reg    o_tx_serial,\n    output reg    o_busy\n);\n\n    // State encoding\n    localparam STATE_IDLE     = 2'd0;\n    localparam STATE_START    = 2'd1;\n    localparam STATE_DATA     = 2'd2;\n    localparam STATE_STOP     = 2'd3;\n\n    reg [1:0]   state, next_state;\n    reg [12:0]  baud_cnt;\n    reg [3:0]   bit_cnt;\n    reg [7:0]   data_latch;\n\n    // Next state logic\n    always @(*) begin\n        case (state)\n            STATE_IDLE:  next_state = (i_tx_start) ? STATE_START : STATE_IDLE;\n            STATE_START: next_state = (baud_cnt == BAUD_RATE_DIVIDER - 1) ? STATE_DATA : STATE_START;\n            STATE_DATA:  next_state = (baud_cnt == BAUD_RATE_DIVIDER - 1 && bit_cnt == 7) ? STATE_STOP : STATE_DATA;\n            STATE_STOP:  next_state = (baud_cnt == BAUD_RATE_DIVIDER - 1) ? STATE_IDLE : STATE_STOP;\n            default:     next_state = STATE_IDLE;\n        endcase\n    end\n\n    // Sequential logic\n    always @(posedge i_clk) begin\n        if (i_reset) begin\n            state       <= STATE_IDLE;\n            baud_cnt    <= 0;\n            bit_cnt     <= 0;\n            data_latch  <= 8'd0;\n            o_busy      <= 1'b0;\n            o_tx_serial <= 1'b1; // Idle is high\n        end else begin\n            state <= next_state;\n\n            case (state)\n                STATE_IDLE: begin\n                    baud_cnt    <= 0;\n                    bit_cnt     <= 0;\n                    o_tx_serial <= 1'b1; // Idle is high\n                    o_busy      <= 1'b0;\n                    if (i_tx_start) begin\n                        data_latch  <= i_data;\n                        o_busy      <= 1'b1;\n                    end\n                end\n\n                STATE_START: begin\n                    o_tx_serial <= 1'b0; // Start bit is low\n                    o_busy      <= 1'b1;\n                    if (baud_cnt == BAUD_RATE_DIVIDER - 1)\n                        baud_cnt <= 0;\n                    else\n                        baud_cnt <= baud_cnt + 1'b1;\n                end\n\n                STATE_DATA: begin\n                    o_tx_serial <= data_latch[bit_cnt];\n                    o_busy      <= 1'b1;\n                    if (baud_cnt == BAUD_RATE_DIVIDER - 1) begin\n                        baud_cnt <= 0;\n                        if (bit_cnt == 7)\n                            bit_cnt <= 0;\n                        else\n                            bit_cnt <= bit_cnt + 1'b1;\n                    end else begin\n                        baud_cnt <= baud_cnt + 1'b1;\n                    end\n                end\n\n                STATE_STOP: begin\n                    o_tx_serial <= 1'b1; // Stop bit is high\n                    o_busy      <= 1'b1;\n                    if (baud_cnt == BAUD_RATE_DIVIDER - 1)\n                        baud_cnt <= 0;\n                    else\n                        baud_cnt <= baud_cnt + 1'b1;\n                end\n\n                default: begin\n                    o_tx_serial <= 1'b1;\n                    o_busy      <= 1'b0;\n                end\n            endcase\n        end\n    end\n\nendmodule\n```\n---\nHere are CRITICAL compatibility rules for the `iverilog` compiler that you MUST follow:\n---\n# Icarus Verilog (iverilog) Compatibility Rules\n\nHere are critical rules for writing SystemVerilog that is compatible with the `iverilog` compiler.\n\n1.  **`task` vs. `function` for Time Consumption:**\n    *   A `function` must execute in zero time. It CANNOT contain time-consuming statements like delays (`#10`), event controls (`@(posedge clk)`), or `wait` statements. Use functions for immediate calculations.\n    *   A `task` is used for procedures that consume time. Any procedure that needs to wait for a clock edge or has a delay MUST be a `task`.\n\n2.  **`for` Loop Variable Declaration:**\n    *   DO NOT declare loop variables inside the `for` loop declaration (e.g., `for (int i = 0; ...)`). This is not fully supported.\n    *   You MUST declare the loop variable as an `integer` before the loop begins (e.g., `integer i; for (i = 0; ...)`).\n\n3.  **VCD Dumping:**\n    *   Use the standard `$dumpfile(\"filename.vcd\");` and `$dumpvars(0, module_instance);` system tasks to create waveform dumps.\n---\nBased on all the information above, generate ONLY the SystemVerilog testbench code. Do not add any explanations. The output must be a single, complete SystemVerilog code block.": "```systemverilog\n`timescale 1ns/1ps\n\nmodule uart_tx_tb;\n\n    // Parameters\n    parameter BAUD_RATE_DIVIDER = 16; // Use small divider for fast simulation\n    parameter CLK_PERIOD = 20;        // 50MHz clock (20ns period)\n\n    // DUT ports\n    reg         i_clk;\n    reg         i_reset;\n    reg  [7:0]  i_data;\n    reg         i_tx_start;\n    wire        o_tx_serial;\n    wire        o_busy;\n\n    // Instantiate DUT\n    uart_tx #(\n        .BAUD_RATE_DIVIDER(BAUD_RATE_DIVIDER)\n    ) dut (\n        .i_clk(i_clk),\n        .i_reset(i_reset),\n        .i_data(i_data),\n        .i_tx_start(i_tx_start),\n        .o_tx_serial(o_tx_serial),\n        .o_busy(o_busy)\n    );\n\n    // Clock generation\n    initial begin\n        i_clk = 1'b0;\n        forever #(CLK_PERIOD/2) i_clk = ~i_clk;\n    end\n\n    // VCD Dump\n    initial begin\n        $dumpfile(\"uart_tx_tb.vcd\");\n        $dumpvars(0, uart_tx_tb);\n    end\n\n    // UART RX Model - checks format: 1 start (0), 8 data, 1 stop (1)\n    reg [9:0] rx_shift;\n    integer   rx_idx;\n    reg [7:0] received_data;\n    reg       rx_error;\n    reg       rx_ready;\n\n    task uart_rx_check(input [7:0] expected_data);\n        integer i;\n        begin\n            rx_shift = 'b0;\n            rx_error = 0;\n            rx_ready = 0;\n            // Wait for line idle (high)\n            @(posedge i_clk);\n            while (o_tx_serial !== 1'b1) @(posedge i_clk);\n\n            // Wait for start bit (falling edge)\n            @(negedge o_tx_serial);\n\n            // Wait one half BAUD to sample mid-bit for robust start detection\n            repeat (BAUD_RATE_DIVIDER/2) @(posedge i_clk);\n\n            // Sample 1 start + 8 data + 1 stop bits\n            for (rx_idx = 0; rx_idx < 10; rx_idx = rx_idx + 1) begin\n                rx_shift[rx_idx] = o_tx_serial;\n                repeat (BAUD_RATE_DIVIDER) @(posedge i_clk);\n            end\n\n            // Check start bit\n            if (rx_shift[0] !== 1'b0) begin\n                $display(\"ERROR: Start bit incorrect! Got %b\", rx_shift[0]);\n                rx_error = 1;\n            end\n            // Get data\n            received_data = rx_shift[8:1];\n\n            // Check stop bit\n            if (rx_shift[9] !== 1'b1) begin\n                $display(\"ERROR: Stop bit incorrect! Got %b\", rx_shift[9]);\n                rx_error = 1;\n            end\n            // Check data\n            if (received_data !== expected_data) begin\n                $display(\"ERROR: Data mismatch. Expected %02X, got %02X\", expected_data, received_data);\n                rx_error = 1;\n            end\n            else if (!rx_error) begin\n                $display(\"PASS: Sent=0x%02X  Received=0x%02X\", expected_data, received_data);\n            end\n            rx_ready = 1'b1;\n        end\n    endtask\n\n    // Stimulus\n    initial begin\n        // Initialize signals\n        i_reset    = 1'b1;\n        i_data     = 8'd0;\n        i_tx_start = 1'b0;\n\n        // Hold reset for 2 clocks\n        repeat (2) @(posedge i_clk);\n        i_reset = 1'b0;\n\n        // Test 1: Transmit single byte 0xA5\n        @(posedge i_clk);\n        i_data = 8'hA5;\n        i_tx_start = 1'b1;\n        @(posedge i_clk);\n        i_tx_start = 1'b0;\n\n        uart_rx_check(8'hA5);\n\n        // Test 2: Wait idle, transmit 0x00\n        repeat (BAUD_RATE_DIVIDER*4) @(posedge i_clk); // Wait a few IDLE bits\n\n        i_data = 8'h00;\n        i_tx_start = 1'b1;\n        @(posedge i_clk);\n        i_tx_start = 1'b0;\n\n        uart_rx_check(8'h00);\n\n        // Test 3: Transmit 0xFF\n        repeat (BAUD_RATE_DIVIDER*4) @(posedge i_clk);\n\n        i_data = 8'hFF;\n        i_tx_start = 1'b1;\n        @(posedge i_clk);\n        i_tx_start = 1'b0;\n\n        uart_rx_check(8'hFF);\n\n        // Test 4: Back-to-back byte test (i_tx_start during busy)\n        repeat (BAUD_RATE_DIVIDER*4) @(posedge i_clk);\n\n        i_data = 8'h55;\n        i_tx_start = 1'b1;\n        @(posedge i_clk);\n        i_tx_start = 1'b0;\n\n        // Try to start second byte while first is transmitting (should be ignored)\n        repeat (BAUD_RATE_DIVIDER*3) @(posedge i_clk);\n        i_data = 8'h3C;\n        i_tx_start = 1'b1;\n        @(posedge i_clk);\n        i_tx_start = 1'b0;\n\n        uart_rx_check(8'h55);\n        // Now transmit 0x3C for real\n        repeat (BAUD_RATE_DIVIDER*4) @(posedge i_clk);\n\n        i_data = 8'h3C;\n        i_tx_start = 1'b1;\n        @(posedge i_clk);\n        i_tx_start = 1'b0;\n\n        uart_rx_check(8'h3C);\n\n        // Test 5: Check o_busy behavior\n        repeat (BAUD_RATE_DIVIDER*2) @(posedge i_clk);\n\n        i_data = 8'hDE;\n        i_tx_start = 1'b1;\n        @(posedge i_clk);\n        i_tx_start = 1'b0;\n\n        // o_busy should go high soon after i_tx_start\n        @(posedge i_clk);\n        if (o_busy !== 1'b1)\n            $display(\"ERROR: o_busy not asserted during TX!\");\n\n        // Wait until transmission finishes\n        wait (o_busy === 1'b0);\n        $display(\"o_busy deasserted after transmission as expected.\");\n\n        uart_rx_check(8'hDE);\n\n        // Test 6: Reset handling\n        repeat (BAUD_RATE_DIVIDER*2) @(posedge i_clk);\n\n        i_data = 8'h33;\n        i_tx_start = 1'b1;\n        @(posedge i_clk); // Start a transmission\n        i_tx_start = 1'b0;\n\n        // Apply reset in the middle of transmission\n        repeat (BAUD_RATE_DIVIDER*3/2) @(posedge i_clk);\n        i_reset = 1'b1;\n        @(posedge i_clk);\n        i_reset = 1'b0;\n        @(posedge i_clk);\n\n        // With reset, transmission should halt, output reset to IDLE (high)\n        if (o_tx_serial !== 1'b1)\n            $display(\"ERROR: o_tx_serial not returning HIGH after reset!\");\n        if (o_busy !== 1'b0)\n            $display(\"ERROR: o_busy not cleared after reset!\");\n        else\n            $display(\"Reset during TX handled as expected.\");\n\n        // Now start another transmission\n        repeat (BAUD_RATE_DIVIDER*2) @(posedge i_clk);\n        i_data = 8'hBE;\n        i_tx_start = 1'b1;\n        @(posedge i_clk);\n        i_tx_start = 1'b0;\n        uart_rx_check(8'hBE);\n\n        // Done\n        repeat (BAUD_RATE_DIVIDER*4) @(posedge i_clk);\n        $display(\"All TB tests finished.\");\n        $finish;\n    end\n\nendmodule\n```"
}